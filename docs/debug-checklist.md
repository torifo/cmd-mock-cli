# cmdock デバッグ・未検証チェックリスト

自動テストで通っているが、実際の TUI や E2E 動作として未確認の項目をまとめる。

---

## 1. TUI 操作（手動テスト未実施）

| # | 操作 | 期待動作 | 確認済 |
|---|------|---------|--------|
| 1 | オンボーディング完全フロー（Demo→SelectMode→SelectDifficulty→ゲーム開始） | 各ステップが正しく表示・遷移する | ☐ |
| 2 | Tab キーで補完候補を表示 | 候補リストが入力欄に表示される | ☐ |
| 3 | Tab 連打で候補が循環する | 2回目で次候補、末尾で先頭に戻る | ☐ |
| 4 | Shift+Tab / ↑ で前候補に戻る | 逆方向に循環する | ☐ |
| 5 | ↓ で次候補に進む | 循環する | ☐ |
| 6 | Tab で候補確定 → 入力欄に反映 | 選択候補がスペース付きで入力欄に入る | ☐ |
| 7 | ← → キーでカーソル移動 | カーソルが文字単位で移動する | ☐ |
| 8 | Home / End キー | 行頭・行末にジャンプする | ☐ |
| 9 | Delete キー（カーソル右を削除） | カーソル位置の文字が消える | ☐ |
| 10 | Backspace で多バイト文字削除（例: 日本語） | 1文字分正しく削除される（コード修正済、未手動確認） | ☐ |
| 11 | Esc キー（入力あり） | 入力がクリアされる | ☐ |
| 12 | Esc キー（入力なし） | セッション保存して終了する | ☐ |
| 13 | Ctrl+C | セッション保存して終了する | ☐ |
| 14 | Ctrl+D（入力空のとき） | セッション保存して終了する | ☐ |
| 15 | ターミナルリサイズ | TUI が再描画される（クラッシュしない） | ☐ |
| 16 | ターミナルが極端に小さい（20×5など） | パニックしない | ☐ |

---

## 2. ゲーム進行（ユニットテスト済だが E2E 未確認）

| # | シナリオ | 期待動作 | 確認済 |
|---|---------|---------|--------|
| 17 | クイズモード：正解コマンドを入力 | "correct" + explanation が表示される | ☐ |
| 18 | クイズモード：同義コマンドを入力（例: `docker image ls`） | "correct" として判定される | ☐ |
| 19 | クイズモード：不正解コマンドを入力 | "not quite" + hint が表示される | ☐ |
| 20 | クイズモード：全問回答後に問題がローテートする | 問題がループして最初に戻る | ☐ |
| 21 | challenge モード：複数コマンドを記録して `submit` | 判定結果が表示される | ☐ |
| 22 | challenge モード：`submit` で失敗判定 | "challenge failed" + hint が表示される | ☐ |
| 23 | challenge モード以外で `submit` | エラーメッセージが表示される | ☐ |
| 24 | `result` コマンド：数問解答後に表示 | 正答率・モード別・難易度別が表示される | ☐ |
| 25 | `resume` コマンド：Ctrl+C 後に再起動して resume | 前回の状態が復元される | ☐ |
| 26 | `help` コマンド | ヘルプテキストが表示される | ☐ |

---

## 3. CLI フラグ（起動時の動作未確認）

| # | フラグ | 期待動作 | 確認済 |
|---|-------|---------|--------|
| 27 | `cmdock --learning-mode docker` | Docker モードで起動、docker 問題が出る | ☐ |
| 28 | `cmdock --learning-mode macos` | macOS モードで起動する | ☐ |
| 29 | `cmdock --difficulty hard` | ヒントなし・広い出題範囲 | ☐ |
| 30 | `cmdock --difficulty normal` | 最小ヒント | ☐ |
| 31 | `cmdock --play-mode challenge` | challenge モードで起動する | ☐ |
| 32 | `cmdock --no-completion` | 補完候補が表示されない | ☐ |
| 33 | `cmdock --list` | 利用可能オプション一覧が出力される | ☐ |
| 34 | `cmdock --play-mode quiz --difficulty easy` | オンボーディングをスキップしてゲーム開始 | ☐ |
| 35 | `cmdock --config path/to/config.toml` | カスタム設定ファイルが読み込まれる | ☐ |

---

## 4. 自動テストが存在しない関数・パス

| # | 対象 | 内容 | テスト追加済 |
|---|------|------|------------|
| 36 | `completion_suggestions()` | 補完候補の生成ロジック | ☐ |
| 37 | `apply_cli_overrides()` | CLI フラグの state への適用 | ☐ |
| 38 | `record_result()` | 統計への正誤記録 | ☐ |
| 39 | `result_text()` | 成績テキスト生成 | ☐ |
| 40 | `handle_meta_command("help")` | help 出力 | ☐ |
| 41 | `handle_meta_command("resume")` | セッション復元パス | ☐ |
| 42 | `handle_meta_command("submit")` | submit 判定パス | ☐ |
| 43 | `SessionStore::save / load` | ファイルへの JSON 永続化往復 | ☐ |
| 44 | `AppConfig::load_or_default` | config.toml の読み込み | ☐ |
| 45 | `DockerSim::exec()` | exec コマンドの動作 | ☐ |
| 46 | `DockerSim::logs()` | logs コマンドの動作 | ☐ |
| 47 | `UiState::apply_completion()` | Tab 補完確定ロジック | ☐ |
| 48 | `onboarding_prompt_lines()` 各ステップ | プロンプト文字列の内容 | ☐ |
| 49 | `process_onboarding()` の各ステップ | Demo / SelectMode / SelectDifficulty 遷移 | ☐ |

---

## 5. エッジケース（未検証）

| # | シナリオ | 期待動作 | 確認済 |
|---|---------|---------|--------|
| 50 | メタコマンドに似た入力（例: `quit something`） | 不明コマンドとして処理される | ☐ |
| 51 | 非常に長い入力（100文字超） | TUI が崩れない | ☐ |
| 52 | セッションファイルが壊れている状態で起動 | クラッシュせずデフォルト状態で起動 | ☐ |
| 53 | 空の入力で Enter | 何も起きない（ログに追加されない） | ☐ |
| 54 | 問題バンクが 1 問のとき | % で正しくローテートする | ☐ |
| 55 | `find . -name app.log -type f` | `-type` フラグが無視されて名前で検索される | ☐ |
| 56 | `grep pattern` （ファイル引数なし） | VFS 全体からパターン検索する | ☐ |
| 57 | オンボーディング中に無効な入力（SelectMode で `3`） | "Enter 1 or 2" メッセージが表示される | ☐ |
| 58 | 保存済みセッションがある状態で起動 | オンボーディングをスキップして前回のゲームから再開 | ☐ |

---

*このリストは `cargo test` がカバーしない手動・統合テストの指針として使う。*
*確認できたら ☐ → ☑ に更新する。*

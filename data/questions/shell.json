{
  "questions": [
    {
      "prompt": "現在のディレクトリ配下から `app.log` を探すコマンドを打ってください。",
      "required_tokens": ["find", ".", "app.log"],
      "synonyms": ["find . -name app.log"],
      "explanation": "`find` は条件に合うパスを再帰的に探します。",
      "hint": "ヒント: `find <開始位置> -name <名前>` の形を思い出してください。"
    },
    {
      "prompt": "`readme.txt` の内容を表示するコマンドを打ってください。",
      "required_tokens": ["cat", "readme.txt"],
      "synonyms": ["cat ./readme.txt"],
      "explanation": "`cat` はファイル内容の表示に使います。",
      "hint": "ヒント: 内容をそのまま標準出力へ出す基本コマンドです。"
    },
    {
      "prompt": "`backup` ディレクトリを作るコマンドを打ってください。",
      "required_tokens": ["mkdir", "backup"],
      "synonyms": ["mkdir ./backup"],
      "explanation": "`mkdir` はディレクトリを新規作成します。",
      "hint": "ヒント: make directory の略です。"
    },
    {
      "prompt": "現在のディレクトリで隠しファイルも含めて一覧表示するコマンドを打ってください。",
      "required_tokens": ["ls", "-a"],
      "synonyms": ["ls --all"],
      "explanation": "`ls -a` は隠しファイルを含めて一覧表示します。",
      "hint": "ヒント: all を意味する短いオプションを付けます。"
    },
    {
      "prompt": "`/var/log/app.log` の中から `error` を含む行を探すコマンドを打ってください。",
      "required_tokens": ["grep", "error", "/var/log/app.log"],
      "synonyms": ["grep error /var/log/app.log"],
      "explanation": "`grep` は文字列を含む行を検索するコマンドです。",
      "hint": "ヒント: `grep <検索語> <ファイル>` の順です。"
    },
    {
      "prompt": "`backup/logs/2026` まで親ディレクトリごとまとめて作成するコマンドを打ってください。",
      "required_tokens": ["mkdir", "-p", "backup/logs/2026"],
      "synonyms": ["mkdir -p ./backup/logs/2026"],
      "explanation": "`mkdir -p` は途中の親ディレクトリもまとめて作成できます。",
      "hint": "ヒント: 親ディレクトリも同時に作るオプションを使います。"
    },
    {
      "prompt": "`backup` ディレクトリを中身ごと削除するコマンドを打ってください。",
      "required_tokens": ["rm", "-r", "backup"],
      "synonyms": ["rm -r ./backup"],
      "explanation": "`rm -r` はディレクトリを再帰的に削除します。",
      "hint": "ヒント: recursive の `-r` を付けます。"
    },
    {
      "prompt": "`readme.txt` を `backup/readme.txt` にコピーするコマンドを打ってください。",
      "required_tokens": ["cp", "readme.txt", "backup/readme.txt"],
      "synonyms": ["cp ./readme.txt backup/readme.txt"],
      "explanation": "`cp` はファイルやディレクトリを複製します。",
      "hint": "ヒント: `cp <元> <先>` の順です。"
    }
  ],
  "challenges": [
    {
      "prompt": "課題: `logs` ディレクトリを作成し、`readme.txt` を `logs/readme.bak` にコピーしてから `submit` してください。",
      "required_steps": [
        ["mkdir", "logs"],
        ["cp", "readme.txt", "logs/readme.bak"]
      ],
      "forbidden_tokens": ["rm"],
      "hint": "ヒント: 2手必要です。ディレクトリ作成とコピーを順に行ってください。"
    },
    {
      "prompt": "課題: `notes.txt` を作成し、その後 `archive` ディレクトリを作って `archive/notes.txt` に移動してから `submit` してください。",
      "required_steps": [
        ["touch", "notes.txt"],
        ["mkdir", "archive"],
        ["mv", "notes.txt", "archive/notes.txt"]
      ],
      "forbidden_tokens": ["rm"],
      "hint": "ヒント: ファイル作成、保存先作成、移動の3手です。"
    },
    {
      "prompt": "課題: `backup/logs` を親ディレクトリごと作成し、`/var/log/app.log` を `backup/logs/app.log` にコピーしてから `submit` してください。",
      "required_steps": [
        ["mkdir", "-p", "backup/logs"],
        ["cp", "/var/log/app.log", "backup/logs/app.log"]
      ],
      "forbidden_tokens": ["rm"],
      "hint": "ヒント: 先にネストしたディレクトリを作ってからコピーします。"
    },
    {
      "prompt": "課題: 隠しファイルも含めて現在のディレクトリ一覧を確認し、その後 `app.log` を探してから `submit` してください。",
      "required_steps": [
        ["ls", "-a"],
        ["find", ".", "app.log"]
      ],
      "forbidden_tokens": ["rm"],
      "hint": "ヒント: 一覧確認のあと、再帰検索を行う2手です。"
    }
  ]
}

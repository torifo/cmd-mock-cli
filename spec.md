# プロジェクト仕様書: CLI Linux / Docker コマンド学習ゲーム

## 1. 目的

本プロジェクトは、Linux および Docker の CLI コマンドを、ユーザーが調べなくても十分に使える状態へ近づけるための学習用ゲームである。

最低到達ラインは以下とする。

- 各コマンドが何を意味するかを理解できる
- 代表的な引数やオプションの役割を理解できる
- 補完ありの支援付きで正しく打てる
- 最終的に補完なしでも打てる

単なる暗記ではなく、手を動かして入力し、実際の操作順序や文脈とともに覚えられることを重視する。

## 2. 基本方針

### 2.1 安全性

本アプリは必ず Simulation Mode で動作する。

- 実際のファイルシステムや Docker 環境には一切変更を加えない
- ユーザー入力を `std::process::Command` で直接実行しない
- すべてのコマンドはアプリ内の仮想シェルで解析し、仮想状態のみを更新する

### 2.2 学習体験

- CLI としてコマンドを実際に入力させる
- 補完あり / 補完なしを切り替え可能にする
- 難易度に応じてヒント量、出題範囲、解説量を制御する
- 正解コマンドだけでなく、同義コマンドや近い表現も比較表示する

### 2.3 段階的リリース

MVP から Docker を含めるが、コマンド数や課題数は段階的に増やす。

- MVP: Linux 基礎 + Docker 初歩
- 拡張: コマンド追加、課題追加、補完精度向上、統計分析の強化

## 3. 対象プラットフォーム

### 3.1 実装言語

- Rust

### 3.2 対応 OS

- macOS
- Linux
- Windows

### 3.3 学習対象モード

実行環境の OS 自動判定だけに依存せず、ユーザーが学習対象を選べるようにする。

- `linux`
- `macos`
- `docker`

補足:

- 実行環境の情報は取得してよい
- ただし出題や補完は「実行中のOS」より「学習対象モード」を優先する
- Docker モードでは Linux 系コマンドを前提に扱ってよい

## 4. 提供モード

### 4.1 プレイモード

以下の 2 モードを提供する。

#### クイズモード

- 問題文に対して、適切なコマンドをユーザーが入力する
- 出題形式は「この状況を満たすコマンドを打て」に統一する
- コマンドの意味や用途を思い出して打つ訓練を目的とする

#### 課題解決モード

- ユーザーは複数ステップの操作を自力で進める
- 各課題には期待されるコマンドフローがある
- 課題完了後、ユーザーが `submit` を入力して判定する

### 4.2 補完モード

補完の有無は難易度とは分離し、モードとして切り替える。

- `completion:on`
- `completion:off`

補完ありは学習支援用、補完なしは定着確認用とする。

## 5. 難易度

難易度はヒント量と出題範囲で分ける。補完の有無は難易度に含めない。

### easy

- ヒント多め
- 出題範囲は基礎コマンド中心
- 解説は詳細
- 補完候補の情報量も多め

### normal

- ヒント少なめ
- 出題範囲は基礎 + 一部応用
- 解説は標準量

### hard

- ヒントなし、または最小限
- 出題範囲は広い
- 解説は簡潔
- ユーザーが自力で思い出すことを優先

## 6. コア機能

### 6.1 仮想シェル

ユーザーの入力行を受け取り、以下を行う。

- トークン分割
- コマンド判定
- オプション / 引数解析
- 学習対象モードに応じた妥当性チェック
- 仮想状態更新
- 判定ログ保存

### 6.2 仮想ファイルシステム

仮想ファイルシステムをメモリ上で持つ。

最低限必要な要素:

- 現在ディレクトリ
- ディレクトリとファイルの区別
- ファイル内容
- パス解決
- 作成、移動、コピー、削除

推奨内部構造:

- 木構造のノードベース
- `File`
- `Directory`
- 必要に応じて `Symlink` を将来拡張で追加

`HashMap<String, Vec<String>>` のような単純表現ではなく、状態遷移や判定に耐える構造を採用する。

### 6.3 仮想 Docker 環境

Docker モード用に、アプリ内に仮想 Docker 状態を持つ。

最低限必要な要素:

- images
- containers
- container state
- container names
- logs の疑似内容

初期段階からなるべく多くの初歩的コマンドを扱えるようにするが、MVP では以下を優先する。

- `docker images`
- `docker pull`
- `docker run`
- `docker ps`
- `docker stop`
- `docker rm`
- `docker logs`
- `docker exec`

将来拡張候補:

- `docker build`
- `docker compose`
- `docker start`
- `docker restart`
- `docker inspect`

## 7. 出題と判定

### 7.1 クイズモードの判定

判定は完全文字列一致ではなく、以下をすべて満たす場合に正解とする。

- 必須トークンを含む
- 並び順が実際に動作する自然な順序である
- 学習対象モードに照らして妥当である

例えば、順不同でトークンだけ含んでいても、CLI として意味が壊れる並びなら不正解とする。

判定要素:

- コマンド名
- サブコマンド
- 主要オプション
- 必須引数
- 順序妥当性

正解後に表示する内容:

- 正解例
- 同義コマンド、または近い別解
- 各コマンドの違い
- 主要オプションの意味

### 7.2 課題解決モードの判定

課題解決モードは最終状態のみではなく、コマンドフローで判定する。

- 各課題に期待する操作ステップ列を持つ
- ユーザー操作の履歴を記録する
- `submit` 入力時に履歴を照合して評価する

判定方針:

- 必須ステップを満たしているか
- 明らかに誤った手順が含まれていないか
- 同義コマンドによる代替を許容するか
- 不要な余分操作をどこまで許容するか

初期実装では、厳しすぎる完全順序一致は避け、以下を推奨とする。

- 必須ステップの部分順序一致
- 同義コマンド許容
- 重大な誤操作のみ失敗扱い

## 8. コマンド補完

### 8.1 補完対象

補完ありモードでは、以下を候補とする。

- コマンド名
- サブコマンド
- 代表的オプション
- パス
- Docker リソース名

### 8.2 難易度別の補完情報量

#### easy

- 候補を広く表示
- 説明付き表示を許容
- 次に来やすい引数も提案してよい

#### normal

- 候補数を絞る
- 説明は簡潔

#### hard

- 補完ありモードであっても候補数を最小限にする
- 説明はほぼ出さない

### 8.3 UI要件

- Tab キーで補完
- 候補リストを画面下部または入力欄近くに表示
- 候補選択時に現在入力に反映
- 候補が複数ある場合はナビゲーション可能

## 9. セッション管理

### 9.1 永続化

軽量な永続化を持つ。

- セッションの途中状態を保存できる
- 過去の成績を保持する

### 9.2 コマンド

- `resume`: 保存済みセッションを再開する
- `result`: 成績や進捗を表示する

MVP では単一のデフォルトプロファイルを採用する。

- 1 ユーザー = 1 プロファイル
- 将来拡張で複数スロット対応を検討

### 9.3 保存対象

- 現在の学習モード
- 難易度
- 補完設定
- 進行中セッション情報
- 問題履歴
- 正誤履歴
- 苦手コマンド集計

## 10. 結果表示

`result` では最低限以下を表示する。

- 正答率
- モード別進捗
- 難易度別進捗
- よく間違えるコマンド
- よく間違えるオプションや引数

将来拡張候補:

- 使用頻度の高いコマンド
- 連続正解数
- 推奨復習リスト

## 11. TUI 要件

本命 UI は対話型 TUI とする。まずはプロトタイプとして、以下の構成を推奨する。

- 問題文または課題説明ペイン
- 入力欄
- 補完候補ペイン
- フィードバック / 履歴ペイン
- ステータスバー

ステータスバー表示候補:

- 学習対象モード
- プレイモード
- 難易度
- 補完設定
- 現在の問題番号

使用候補クレート:

- `ratatui`
- `crossterm`

## 12. 設定

起動引数に加え、設定ファイルを持てるようにする。

推奨ファイル:

- `config.toml`

推奨設定項目:

- `default_learning_mode`
- `default_play_mode`
- `default_difficulty`
- `completion`
- `show_examples`
- `show_synonyms`

起動引数は設定ファイルを上書きできるようにする。

## 13. 対応コマンド方針

### 13.1 Linux / macOS 系

初期対象は段階的に増やす前提とし、まずは以下を優先する。

- `pwd`
- `ls`
- `cd`
- `mkdir`
- `touch`
- `cat`
- `cp`
- `mv`
- `rm`
- `find`
- `grep`
- `echo`

将来拡張候補:

- `chmod`
- `chown`
- `head`
- `tail`
- `less`
- `wc`
- `ps`
- `kill`
- `tar`
- `curl`

### 13.2 Docker 系

MVP に Docker を含める。

優先コマンド:

- `docker images`
- `docker pull`
- `docker run`
- `docker ps`
- `docker stop`
- `docker rm`
- `docker logs`
- `docker exec`

段階追加候補:

- `docker build`
- `docker compose`
- `docker start`
- `docker restart`
- `docker inspect`

## 14. アーキテクチャ推奨

以下の責務分離を推奨する。

- `parser`: 入力解析
- `command_registry`: コマンド定義とメタデータ
- `executor`: 仮想シェル実行
- `vfs`: 仮想ファイルシステム
- `docker_sim`: 仮想 Docker 状態
- `quiz_engine`: クイズ出題と判定
- `challenge_engine`: 課題解決と `submit` 判定
- `completion`: 補完機能
- `session_store`: 保存 / 復元 / 成績
- `ui`: TUI

コマンド補完は差し替えしやすくするため、以下のような抽象化を推奨する。

- `CompletionEngine` トレイト
- `EnabledCompletionEngine`
- `DisabledCompletionEngine`

## 15. MVP 範囲

MVP では以下を必須とする。

- 対話型 TUI の最小プロトタイプ
- クイズモード
- 課題解決モード
- 補完 on/off 切り替え
- easy / normal / hard
- 仮想ファイルシステム
- Docker 初歩コマンド群
- `resume`
- `result`
- 正解時の同義コマンド表示

MVP では後回しにしてよいもの:

- 高度な Docker 構築系
- compose の詳細再現
- 複数ユーザープロファイル
- 高度な統計可視化

## 16. テスト方針

最低限以下をテスト対象にする。

- パーサ
- コマンド判定
- 仮想ファイルシステム遷移
- 仮想 Docker 状態遷移
- クイズ判定
- 課題解決の `submit` 判定
- 補完候補生成
- セッション保存 / 復元

推奨:

- `cargo test`
- `cargo clippy`
- 必要に応じて TUI のスナップショットテスト

## 17. CI/CD

### テスト / 品質

- `cargo test`
- `cargo clippy`
- `cargo fmt --check`

### リリース

タグ作成時に以下を自動化する。

- Linux 向けバイナリビルド
- macOS 向けバイナリビルド
- Windows 向けバイナリビルド
- GitHub Release へのアップロード

## 18. 今回の仕様確定事項

今回の会話で確定した内容を明文化する。

- 目的は「意味と引数を理解し、最終的に補完なしでも打てること」
- プレイモードはクイズと課題解決の 2 種類
- 補完の有無は難易度ではなく独立モード
- 難易度は主にヒント量と出題範囲で調整
- クイズは必ずユーザーにコマンドを打たせる
- クイズ判定は同義コマンドを許容する
- ただし順序は実際に通る自然な並びでなければならない
- 課題解決モードはコマンド履歴ベースで判定し、`submit` で終了判定する
- Docker は MVP に含める
- セッション継続は `resume`、履歴確認は `result`
- UI は対話型 TUI を本命とする

